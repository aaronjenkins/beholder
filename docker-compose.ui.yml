version: '3.8'
services:
  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
      args:
        VITE_API_BASE: ${VITE_API_BASE:-''}
    container_name: beholder-ui
    restart: unless-stopped
    ports:
      - "8081:80"
